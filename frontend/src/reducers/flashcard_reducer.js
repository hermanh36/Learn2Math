import {
    RECEIVE_FLASHCARD,
    RECEIVE_FLASHCARDS,
    REMOVE_FLASHCARD
} from '../actions/flashcard_actions';

const FlashcardReducer = (state={}, action) => {

}

export default FlashcardReducer;